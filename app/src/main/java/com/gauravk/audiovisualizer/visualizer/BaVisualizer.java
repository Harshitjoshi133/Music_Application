package com.gauravk.audiovisualizer.visualizer;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class BaVisualizer extends View {
    public BaVisualizer(Context context) {
        this(context, null);
    }

    public BaVisualizer(Context context, AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public BaVisualizer(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
